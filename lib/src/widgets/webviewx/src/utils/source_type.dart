/// Specifies what type is the current source
enum SourceType {
  /// [html] pass a html string
  html,

  /// [url] pass an URL
  url,

  /// [urlBypass] uses bypass to fetch the web page content.
  urlBypass,
}
